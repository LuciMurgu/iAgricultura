# Session: 2026-04-05 — PR-04 Mock Data + Shared Components

## What was done

Executed Prompt 04 from `prompts/PROMPT_SEQUENCE.md`:

1. **Backend API Models Reference** — Created `docs/BACKEND_API_MODELS.py` with 28 Pydantic v2 response models as the Zod schema source of truth.

2. **13 Zod schema files** (`types/`) mirroring backend models 1:1:
   - `common.ts`, `invoices.ts`, `alerts.ts`, `stock.ts`, `dashboard.ts`
   - `parcels.ts`, `cooperative.ts`, `arenda.ts`, `e-transport.ts`
   - `saga-export.ts`, `carbon.ts`, `apia-vault.ts`
   - Type mapping: Decimal→z.number(), UUID→z.string().uuid(), datetime→z.string().datetime(), nullable→.nullable()

3. **12 mock data files** (`lib/mock/data/`) with realistic Romanian agricultural data:
   - 15 invoices from Bayer, Alcedo, Petrom, Pioneer, BASF, Syngenta, Corteva
   - 10 stock items (diesel, fertilizers, herbicides, fungicides, seeds)
   - 8 alerts (overpayment, duplicate, total mismatch)
   - 11 action feed items (all 7 types)
   - 6 parcels near Iași with sensors, weather, APIA compliance
   - 1 cooperative cluster with 8 farmers, 3 bids from Ameropa/Cargill/Bunge
   - 6 lease contracts, 4 e-Transport declarations, 3 carbon parcels, 1 APIA vault

4. **7 shared UI components** (`components/shared/`):
   - `ai-label.tsx` — Sparkle badge + Popover
   - `status-pill.tsx` — 11 status variants with Romanian labels
   - `confidence-badge.tsx` — Color-coded percentage badge
   - `empty-state.tsx` — Zero-data state with CTA
   - `loading-table.tsx` — Skeleton shimmer rows
   - `currency.tsx` — Romanian number formatting (47.580,00 RON)
   - `relative-time.tsx` — Romanian relative timestamps (acum 2h)

5. **Test infrastructure** — Vitest configured with 29 tests validating all mock data against Zod schemas.

## Verification

- `pnpm type-check` — ✅ 0 errors
- `pnpm lint` — ✅ 0 warnings
- `pnpm test` — ✅ 29/29 tests pass

## Issues encountered

- Romanian quotation marks `„..."` break JS string literals (the `"` character is a right double quote). Fixed by using `«...»` guillemets instead.
- Mock alert IDs initially used non-UUID format (`alert-001-...`). Fixed to use proper UUIDs.

## Next

- Prompt 05: Dashboard — Command Center page using the mock data and shared components created here.
