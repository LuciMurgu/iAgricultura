# Session: 2026-04-05 — PR-06 Invoice List + Review (Flagship)

## What was done

Executed Prompt 06 from `prompts/PROMPT_SEQUENCE.md`:

1. **Invoice page** (`app/(auth)/invoices/page.tsx`):
   - PageHeader: "Facturi e-Factura" + ANAF SPV sync badge + count badge
   - Filter bar: status dropdown (6 options) + supplier search (client-side)
   - TanStack Table integration
   - Empty state with contextual messaging
   - Loading skeleton

2. **Invoice table** (`components/invoices/invoice-table.tsx`):
   - TanStack Table with 9 columns: select, expand, #, Furnizor, Linii, Total, Data, Status, Alerte
   - Sortable by date, total (with arrow icon)
   - Expandable row detail showing invoice metadata and line summary
   - Batch select with "Aprobă selecția" action bar
   - Mobile: card-based responsive view (hidden on md+)
   - 44px rows, sticky header, mono font for IDs/CUI, right-aligned numbers

3. **Invoice right rail** (`components/invoices/invoice-right-rail.tsx`):
   - Tab "Alerte": alert cards with price comparison (your price vs regional median, deviation %)
   - Tab "AI": normalization pipeline info (source, model)
   - Tab "Dovezi": document provenance (origin, format, download date, XML link)
   - Tab "Istoric": timeline view (processed → normalized → alerts → pending)

4. **Supporting components**:
   - `invoice-filters.tsx` — Select + Input
   - `invoice-table-skeleton.tsx` — Shimmer loading state
   
5. **Infrastructure**:
   - `hooks/use-invoices.ts` — React Query + feature gate + Zod parse
   - `components/layout/right-rail.tsx` — Updated to be route-aware (renders InvoiceRightRail on /invoices)

## Verification

- `pnpm type-check` — ✅ 0 errors
- `pnpm lint` — ✅ 0 warnings
- `pnpm test` — ✅ 29/29 tests pass
- Dev server: `/invoices` compiles (200, 1170 modules) and renders correctly

## Issues encountered

- React hooks rules violation: `useRightRailStore` was called conditionally inside `RailContent`. Fixed by moving the hook call to the top of the function body.

## Next

Prompt 07: Stock Overview + Alerts
